<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kcc.banking.domain.accountclose.mapper.AccountCloseMapper">

    <insert id="addCancelTrade" parameterType="CloseTrade">
        <selectKey keyProperty="id" resultType="java.lang.Long" order="BEFORE">
            SELECT trade_seq.nextval FROM dual
        </selectKey>
        INSERT INTO TRADE (id, acc_id, emp_id, branch_id, trade_date, amount, balance, trade_type, status, cash_indicator, description, trade_number)
        VALUES (#{id}, #{accId}, #{empId}, #{branchId}, SYSTIMESTAMP, #{amount}, 0, 'CLOSE', 'NOR', 'TRUE', '계좌해지', 13)
    </insert>

    <update id="updateStatus" parameterType="AccountStatus">
        UPDATE ACCOUNT
        SET
            status = #{status},
            expire_date = SYSTIMESTAMP,
            balance = 0,
            modification_date = SYSTIMESTAMP,
            modifier = #{modifier}
        WHERE
            id = #{id}
    </update>
</mapper>
